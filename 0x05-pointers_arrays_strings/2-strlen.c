#include "holberton.h"
#include <stdio.h>
#include <string.h>
/**
*_strlen - function to return the length of a string.
*@s 
* Return: Always (1) (Success)
*/
int _strlen(char *s)
{
	int longitud = strlen (s);
	printf("%d", longitud);	


	return(1);
}