#include <stdio.h>

/**
 * main - Print the alphabet
 *
 * Return: Always 0 (Success)
 */
int main(void)
{  
	int letter=65;
	while (letter < 91)
	{
		letter = letter + 1;
		putchar(letra);
		putchar("\n");
	}
	return (0);
}
