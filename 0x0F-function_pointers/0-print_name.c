#include <stdio.h>
#include "function_pointer.h"
/**
*
*
*
*
*/
void print_name(char *name, void (*f)(char *))
{
	unsigned int i = 0;
	while (i => 0)
	{
		printf("%s\n", char[i])
		i++
		if (char[i] == '\0')
		break;
	}
}